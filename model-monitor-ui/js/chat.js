export function renderChat() { return `<header class="page-header"><div><div class="eyebrow">LIVE TEST</div><h1>实测对话</h1><div class="muted">用相同场景体验模型表现</div></div></header><div class="card chat-card"><label class="field chat-select"><span class="field-label">选择模型</span><select class="select" id="chat-model"><option>GPT-4o</option><option>Claude 3.5 Sonnet</option><option>DeepSeek V3</option><option>Gemini 2.5 Pro</option></select></label><label class="field"><span class="field-label">Prompt</span><textarea class="textarea" id="chat-prompt">请介绍一下量子计算。</textarea></label><button class="btn btn-primary" style="width:100%" id="send-chat">发送</button><div id="chat-messages"></div></div>`; }
export function bindChat(root,{onToast}) { root.querySelector('#send-chat').addEventListener('click',()=>{const btn=root.querySelector('#send-chat'), prompt=root.querySelector('#chat-prompt').value.trim();if(!prompt){onToast('请输入 Prompt');return;}const out=root.querySelector('#chat-messages');out.innerHTML=`<div class="message user"><div class="message-label">你的 Prompt</div>${prompt}</div><div class="message assistant"><div class="message-label">GPT-4o · <span class="loading-dots">生成中</span></div><div id="chat-answer"></div></div>`;btn.disabled=true;btn.textContent='生成中…';setTimeout(()=>{root.querySelector('#chat-answer').textContent='量子计算是一种利用量子力学原理进行信息处理的计算范式。它通过量子比特的叠加和纠缠，在特定问题上展现出传统计算机难以达到的效率。';root.querySelector('.message-label').innerHTML='GPT-4o · 已完成';btn.disabled=false;btn.textContent='发送';onToast('模型回复完成');},1100);}); }
