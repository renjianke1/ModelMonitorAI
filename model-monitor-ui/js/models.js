import { modelSeries } from './mock-data.js';
export function renderModels() { return `<header class="page-header"><div><div class="eyebrow">MODEL CATALOG</div><h1>模型列表</h1><div class="muted">按系列管理和检测模型</div></div></header><div class="search-row"><input class="input" id="model-search" placeholder="搜索模型"/><button class="btn btn-small btn-outline" id="compare-entry">对比</button></div><div class="sort-row" id="sort-row"><button class="sort-btn active" data-sort="name">名称</button><button class="sort-btn" data-sort="status">状态</button><button class="sort-btn" data-sort="latency">延迟</button><button class="sort-btn" data-sort="channels">渠道数</button></div><div class="toolbar"><button class="btn btn-small" id="expand-all">全部展开</button><button class="btn btn-small" id="collapse-all">全部收起</button><button class="btn btn-small btn-outline" id="flat-view">平铺视图</button></div><div id="series-list">${renderSeries(modelSeries)}</div><div class="entry-actions"><button class="btn btn-primary" data-action="chat-entry">实测对话</button><button class="btn btn-outline" data-action="compare-entry-bottom">模型对比</button></div>`; }
function renderSeries(series) { return series.map((s,i)=>`<section class="card series" data-series="${s.name}" data-open="false"><div class="series-head"><span class="chevron">›</span><div class="series-meta"><strong>${s.name}</strong><small>${s.total} 个模型</small></div><div class="series-health"><span class="status ${s.alive===s.total?'ok':'warn'}">${s.alive} 个存活</span><br><small class="muted">${s.total-s.alive} 个失败</small></div></div><div class="series-body" hidden>${s.models.map((name,j)=>`<div class="model-row"><span></span><div><div class="model-name">${name}</div><div class="model-id">${name.toLowerCase().replaceAll(' ','-')}</div></div><button class="btn btn-small btn-outline" data-detect="${name}">检测</button></div>`).join('')}</div></section>`).join(''); }
export function bindModels(root,{navigate,onToast}) { const list=root.querySelector('#series-list'); const toggle=card=>{card.classList.toggle('open'); card.querySelector('.series-body').hidden=!card.classList.contains('open');}; list.querySelectorAll('.series-head').forEach(h=>h.addEventListener('click',()=>toggle(h.parentElement))); root.querySelector('#expand-all').addEventListener('click',()=>list.querySelectorAll('.series').forEach(c=>{c.classList.add('open');c.querySelector('.series-body').hidden=false;})); root.querySelector('#collapse-all').addEventListener('click',()=>list.querySelectorAll('.series').forEach(c=>{c.classList.remove('open');c.querySelector('.series-body').hidden=true;})); root.querySelector('#flat-view').addEventListener('click',()=>{list.classList.toggle('flat-list'); onToast(list.classList.contains('flat-list')?'已切换平铺视图':'已切换分组视图');}); root.querySelector('#model-search').addEventListener('input',e=>{const q=e.target.value.toLowerCase(); list.querySelectorAll('.series').forEach(c=>{const match=c.textContent.toLowerCase().includes(q); c.hidden=!match; if(q){c.classList.add('open');c.querySelector('.series-body').hidden=false;}});}); root.querySelectorAll('[data-sort]').forEach(btn=>btn.addEventListener('click',()=>{root.querySelectorAll('[data-sort]').forEach(b=>b.classList.remove('active'));btn.classList.add('active');const cards=[...list.querySelectorAll('.series')];const key=btn.dataset.sort;cards.sort((a,b)=>{if(key==='name')return a.dataset.series.localeCompare(b.dataset.series,'zh-CN');const av=modelSeries.find(x=>x.name===a.dataset.series),bv=modelSeries.find(x=>x.name===b.dataset.series);if(key==='status')return (bv.alive/bv.total)-(av.alive/av.total);return bv.total-av.total;}).forEach(c=>list.appendChild(c));onToast(`已按${btn.textContent}排序`);})); root.querySelectorAll('[data-detect]').forEach(b=>b.addEventListener('click',e=>{e.stopPropagation();e.currentTarget.textContent='检测中';setTimeout(()=>{e.currentTarget.textContent='检测';onToast(`${e.currentTarget.dataset.detect} 检测完成`);},700);})); root.querySelector('[data-action="chat-entry"]').addEventListener('click',()=>navigate('chat')); root.querySelector('[data-action="compare-entry-bottom"],#compare-entry').addEventListener('click',()=>navigate('compare')); }

export function renderCompare() { const choices=['GPT-4o','Claude 3.5','DeepSeek V3','Gemini 2.5']; return `<header class="page-header"><div><div class="eyebrow">SIDE BY SIDE</div><h1>模型对比</h1><div class="muted">最多选择 4 个模型</div></div></header><div class="card form-card"><div class="section-heading"><h3>选择模型</h3><span id="compare-count" class="muted">0 / 4</span></div>${choices.map((n,i)=>`<label class="model-row" style="padding:10px 0"><input class="checkbox compare-check" type="checkbox" value="${n}"><span class="model-name">${n}</span><span class="muted">${i===2?'低延迟':''}</span></label>`).join('')}<label class="field" style="margin-top:12px"><span class="field-label">Prompt</span><textarea class="textarea" id="compare-prompt" placeholder="输入一段相同的问题进行对比"></textarea></label><button class="btn btn-primary" style="width:100%" id="start-compare">开始对比</button></div><div id="compare-results" class="section"></div>`; }
export function bindCompare(root,{onToast}) { const checks=[...root.querySelectorAll('.compare-check')]; const count=root.querySelector('#compare-count'); checks.forEach(c=>c.addEventListener('change',()=>{if(checks.filter(x=>x.checked).length>4){c.checked=false;onToast('最多选择 4 个模型');}count.textContent=`${checks.filter(x=>x.checked).length} / 4`;})); root.querySelector('#start-compare').addEventListener('click',()=>{const selected=checks.filter(x=>x.checked).map(x=>x.value);if(!selected.length){onToast('请至少选择一个模型');return;} const out=root.querySelector('#compare-results');out.innerHTML='<div class="section-heading"><h2>对比结果</h2><span class="muted loading-dots">正在测试</span></div>';setTimeout(()=>{out.innerHTML=`<div class="section-heading"><h2>对比结果</h2><span class="muted">已完成</span></div>${selected.map((n,i)=>`<div class="card message assistant"><div class="dead-head"><strong>${n}</strong><span class="status ok">${720+i*180}ms</span></div><p>这是针对相同 Prompt 的模拟回答。模型从不同角度给出了清晰、简洁的结果。</p></div>`).join('')}`;},900);}); }
